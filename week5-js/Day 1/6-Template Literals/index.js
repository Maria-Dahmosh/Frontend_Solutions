//1
const name = "Maria Dahmosh";
const age = 26;
console.log(`My name is ${name} and I'm ${age} years old.`);

//2

const length = 10;
const width = 2;
const area = length * width;

console.log(
  `The area of a rectangle with length of ${length} and width of ${width} is ${area}.`
);
//3
const currentDay = "Sunday";
const task1 = "do my morning SkinCare";
const task2 = "Read a book";
const task3 = "do pilates";

console.log(`On ${currentDay}, I will ${task1}, ${task2} and ${task3}.`);

//4
const num = 1;
console.log(`I have ${num} pet.`);

//5
const base = 10;
const height = 2;
const triangleArea = (base * height) / 2;
console.log(
  `The area of a triangle with a base of ${base} and height of ${height} is ${triangleArea}.`
);
